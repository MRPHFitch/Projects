// pages/burst_receive_page.dart
import 'package:flutter/material.dart';
class BurstPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Burst Transmission')), body: Center(child: Text('Burst Receive Page')));
}